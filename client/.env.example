# Optional. Leave unset for the normal setup, where one Express service serves
# both the API and this app (and the Vite dev server proxies /api to it).
# Set this only if you deploy the frontend and backend to separate hosts.
# VITE_API_BASE_URL=https://your-api.onrender.com/api
